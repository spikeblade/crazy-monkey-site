{
  "name": "crazy-monkey-site",
  "version": "2.3.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/netlify/functions/__tests__/**/*.test.js"],
    "clearMocks": true
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "astro": "^5.7.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
